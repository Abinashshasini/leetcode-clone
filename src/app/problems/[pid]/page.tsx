import Header from '@/components/header/index';
import WorkSpace from '@/components/workspace/index';
import React from 'react';

const PidPage = () => {
  return (
    <main>
      <Header isBackgroundPresent isPidPage />
      <WorkSpace />
    </main>
  );
};

export default PidPage;
